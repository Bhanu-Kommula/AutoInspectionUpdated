# ========================================
# 🌐 API Gateway Configuration (All Requests Go Through Gateway)
# ========================================
REACT_APP_API_GATEWAY_URL=http://localhost:8080

# Auth Endpoints via Gateway (Mapped to User Service /api/users/auth/**)
REACT_APP_AUTH_BASE_URL=http://localhost:8080/api/users/auth

# User Management Endpoints via Gateway (Dealer, Technician, etc.)
REACT_APP_USER_BASE_URL=http://localhost:8080/api/users

# Posts Service (via Gateway)
REACT_APP_POSTS_BASE_URL=http://localhost:8080/api/v1/posts

# Dealer Service (via Gateway)
REACT_APP_DEALER_BASE_URL=http://localhost:8080/api/v1/dealer

# Technician Service (via Gateway)
REACT_APP_TECHNICIAN_BASE_URL=http://localhost:8080/api/v1/technician

# Admin Service (via Gateway)
REACT_APP_ADMIN_BASE_URL=http://localhost:8080/api/v1/admin

# ========================================
# 🔐 Security & Token Storage Keys
# ========================================
REACT_APP_JWT_STORAGE_KEY=accessToken
REACT_APP_REFRESH_TOKEN_KEY=refreshToken
REACT_APP_USER_INFO_KEY=dealerInfo

# ========================================
# 🛠️ Feature Flags & Debug Options
# ========================================
REACT_APP_ENABLE_DEBUG_MODE=false
REACT_APP_ENABLE_ANALYTICS=true
REACT_APP_ENABLE_ERROR_REPORTING=true
REACT_APP_ENABLE_MOCK_DATA=false
REACT_APP_LOG_LEVEL=info

# ========================================
# 📱 App Info
# ========================================
REACT_APP_APP_NAME=All State Auto Inspection
REACT_APP_APP_VERSION=1.0.0
REACT_APP_SUPPORT_EMAIL=support@allstate.com

# ========================================
# 🌐 CORS Frontend Origin (Used in Backend)
# (Keep this synced with ALLOWED_ORIGINS in backend services)
# ========================================
REACT_APP_FRONTEND_BASE_URL=http://localhost:3000
